let height = 10;
let base = 4;
let perimeter = 2*height + 2*base;
let area = height * base;

console.log(`perimeter of rectangular with height ${height} & base ${base} is ${perimeter}`);
console.log(`area of rectangular with height ${height} & base ${base} is ${area}`);