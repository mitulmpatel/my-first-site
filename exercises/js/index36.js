let firstNumber = 10;
let secondNumber = 20;
let isSameNumber = firstNumber==secondNumber;
console.log(`Both variables have the same value: `, isSameNumber);
console.log(`Both variables don't have the same value: `, !isSameNumber);